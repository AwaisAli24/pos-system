CREATE TABLE users(
    id SERIAL PRIMARY KEY,
    username TEXT NOT NULL,
    password TEXT NOT NULL,
    isAdmin BOOLEAN NOT NULL
)
ALTER TABLE sales
ADD COLUMN iscash BOOLEAN;
CREATE TABLE online_payments(
    id SERIAL PRIMARY KEY,
    name TEXT,
    phone_number TEXT,
    sale_id INT,
    FOREIGN KEY (sale_id) REFERENCES sales(id)
)