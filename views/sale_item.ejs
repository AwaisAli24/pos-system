<%- include('header.ejs') %>
    <%- include('sidebar.ejs') %>
        <div class="add">
            <table class="table mt-5">
                <thead>
                    <tr>
                        <th scope="col">#</th>
                        <th scope="col" colspan="2">Name</th>
                        <th scope="col">Quantity</th>
                        <th scope="col">Price</th>
                        <th scope="col">Total</th>
                    </tr>
                </thead>
                <tbody>
                    <%products.forEach((product,index)=>{%>
                        <tr>
                            <th scope="row">
                                <%=index+1%>
                            </th>
                            <td colspan="2">
                                <%=product.name%>
                            </td>
                            <td><%=quantity[index]%></td>
                            <td>
                                <%=product.price%>
                            </td>
                            <td><%=product.price*quantity[index]%></td>

                        </tr>
                        <%});%>
                </tbody>
            </table>
            <h3>Grand Total:<span class="grand-total1"><%=amount%></span></h3>
        </div>
        </div>

        <%- include('footer.ejs') %>